{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "command": "yarn build:bff",
            "options": {
                "cwd": "${workspaceFolder}/../msp_fes"
            },
            "problemMatcher": [
                "$tsc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "label": "build bff"
        }
    ]
}
