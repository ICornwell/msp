{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "command": "yarn build:bff",
            "options": {
                "cwd": "${workspaceFolder}/../msp_fes"
            },
            "problemMatcher": [
                "$tsc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "label": "build bff"
        },
        {
            "type": "shell",
            "command": "yarn build:server",
            "options": {
                "cwd": "${workspaceFolder}/../msp_servicehub"
            },
            "problemMatcher": [
                "$tsc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "label": "build servicehub"
        }
    ]
}
